{
    "version": 2,
    "builds": [
        {
            "src": "api/search.rs",
            "use": "@vercel/rust"
        }
    ],
    "routes": [
        {
            "src": "/api/search",
            "dest": "/api/search"
        }
    ],
    "env": {
        "RUST_LOG": "info"
    }
} 